"""Fig. 2 — 표현 패러다임 비교 (I절, 2단, 1×4 개념도). **미구현 — 설계만 적어 둔다.**

## 무엇을 그리나

같은 작은 장면 하나에 대해 **네 가지 출력 표현**을 나란히 놓는다.

    (a) 면(segmentation)   마스크 → 세선화/watershed → 조각난 인스턴스
                           화살표 설명: "정체성을 후처리가 만든다"
    (b) 질의(query)        고정 개수 query 슬롯 → 폴리라인
                           화살표 설명: "인스턴스 수가 예산에 묶인다"
    (c) 각도 섹터 슬롯      셀에 6개 섹터. 선을 조금 돌렸을 뿐인데 채널이 바뀌는 것을
        (Sat2Graph GTE)    작은 그림 두 개로. 화살표 설명: "타깃이 불연속"
    (d) ours               셀 = 노드 1 + 순서 없는 슬롯 2, 사슬로 이어짐

## 왜 필요한가

이 논문 전체가 "표현의 문제"라는 주장이다. 그 주장이 **수식과 관련연구보다 먼저,
그림 한 장으로 성립**해야 한다. 리뷰어가 Introduction 만 읽고도 기여를 파악하게 만드는 그림.

## 제작 방식 — 코드가 아니다

(a)(b)(d)는 **그리기 도구(PowerPoint/Illustrator)로 만든다.** 실측이 아니라 개념도다.

**단 (c)만 코드가 필요하다.** GTE 인코딩을 짧게 구현해 선을 1도씩 돌려 가며
**타깃 채널 인덱스가 몇 도에서 바뀌는지** 실제로 뽑는다. "섹터 경계를 스치면 타깃이
튄다"가 그림 위의 주장이 아니라 **측정값**이 되면 설득력이 크게 오른다.
이 스크립트는 그 (c) 패널만 만든다.

## (c) 패널 설계

    입력   없음 (합성) — 셀 하나, 선 하나를 각도 0~360도로 1도씩 회전
    계산   GTE 6섹터 인코딩의 타깃 채널 인덱스 · 채널이 바뀌는 각도(경계) 표시
    출력   figure_02c.png / .pdf / figure_02c.csv (각도별 채널 인덱스)
    비교   같은 회전에 대해 **우리 표현의 타깃**(단위 방향 벡터)은 연속임을 옆에 겹쳐 그린다

이 대조가 §III-C(왜 슬롯이 순서 없어야 하는가)와 Table VII 의 "슬롯 배정 = 각도 섹터" 행을
그림으로 예고하는 역할을 한다.

## 상태

**착수 전.** (c) 패널은 학습이나 baseline 없이 지금 바로 만들 수 있다.
나머지 세 패널은 원고 그림 작업에서 만든다 — 저장소가 아니라 슬라이드 파일로 관리한다.
"""

# from stella.paper.figure_base import PlotFigure
#
#
# class SectorDiscontinuityPanel(PlotFigure):
#     """(c) 패널만: GTE 각도 섹터의 타깃 불연속을 회전 각도별로 실측한다."""
#
#     name = "figure_02"
#     NUM_SECTORS = 6        # Sat2Graph GTE 의 셀당 슬롯 수
#     ANGLE_STEP_DEG = 1.0   # 회전 간격
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
