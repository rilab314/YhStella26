"""Fig. 13 — 짧은 선에 손실을 더 주면 무엇이 달라지나 (VIII절, 1단, 막대).
**미구현 — 설계만 적어 둔다.**

## 무엇을 그리나

길이 구간 네 개 × 조건 두 개의 그룹 막대. y축은 **정답 검출률**(그 구간의 정답 칸 중
모델이 실제로 찾아낸 비율). 막대 위에 구간별 상대 변화율을 적는다.

    가로 축   0~20칸 · 20~40칸 · 40~70칸 · 70칸 이상   (1칸 = 4 px)
    두 조건   길이 역가중 **없음** / **있음**(채택)
              — "길이 역가중"은 짧은 선이 든 칸에 손실 가중치를 더 주는 처방
                (코드 이름 `self_slot.length_power`)

## ★ 실측 완료

    파일    docs/figures/fig_length_power.png (이미 있다)
    대조군  E28_combo (길이 역가중만 없고 나머지는 같은 조건) · val 700장
    값      0~20칸  0.166 → **0.200 (+20.5%)**
            20~40칸 0.338 → 0.354 (+4.7%)
            40~70칸 0.334 → 0.351 (+5.1%)
            70칸+   0.446 → 0.440 (−1.3%, 판정 밴드 ±2% 안이라 "변화 없음")

## 왜 필요한가

VIII절의 핵심 주장 — **"이 처방은 재분배이지 전체 상향이 아니다"** 를 막대 네 쌍이 한눈에
보인다. 긴 선(70칸+)이 거의 그대로인 것까지 보여야 "공짜로 얻은 것이 아니라 옮긴 것"이라는
절제된 주장이 선다. 이득만 그리면 과장으로 읽힌다.

## 지면이 모자라면 뺀다

§6.2 감축안 10번 — 이 그림은 **본문 문장 안의 숫자 네 쌍으로 대체 가능**하다(0.22쪽 절약).
그래도 스크립트는 만들어 둔다. 숫자를 본문에 쓰려면 그 숫자의 출처가 있어야 한다.

## 입력·출력

    입력   log/{채택 실행}/metrics.csv · log/E28_combo/metrics.csv
           구간별 정답 검출률은 셀 단위 진단(`stella/eval/cellstat.py`)에서 나온다.
           **metrics.csv 에 구간별 지표가 없으면** 예측 캐시에서 다시 계산한다 —
           그 경우 두 실행의 캐시를 같은 디코더 설정으로 떠야 한다.
    출력   figure_13/figure_13.png · .pdf
           figure_13/figure_13.csv   (구간, 조건, 검출률, 상대 변화율, 표본 칸 수)
    규격   두 실행 모두 **U 규격**(3,000장·10에폭)이다. 캡션에 밝힌다 —
           F 규격 값과 섞어 읽으면 안 된다.

## 상태

**실측 완료 · 재현 스크립트 미구현.** 현재 저장소의 PNG 는 세션 스크래치에서 만든 것이라
재현 스크립트가 없다 — 그 구멍을 메운다.
"""

# from stella.paper.figure_base import PlotFigure
#
#
# class LengthReweightFigure(PlotFigure):
#     """길이 구간별 정답 검출률: 길이 역가중 있음 / 없음."""
#
#     name = "figure_13"
#     BUCKETS = ((0, 20), (20, 40), (40, 70), (70, 10**9))   # 단위: 칸
#     CONTROL_RUN = "E28_combo"    # 길이 역가중만 없는 대조군
#     BAND = 0.02                  # 판정 밴드 ±2% — 이 안이면 "변화 없음"으로 표시
#     width_in, height_in = 3.5, 2.4   # 1단
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
