"""Table I — 표현 방식 비교 매트릭스 (II절 Related Work 끝, 2단).
**미구현 — 설계만 적어 둔다.**

## 무엇이 들어가나

행 = 방법 갈래 7개, 열 = 표현의 성질 7개.

| 방법 | 1차 단위 | 정체성 | 연결성 | 순서 모호성 | 후처리 | 클래스 | 인스턴스 수 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CLRNet 계열 | 앵커 라인 | 모델 | (없음) | 시점 가정 | NMS | ○ | 고정 소수 |
| MapTR(v2) | 질의 폴리라인 | 모델 | (없음) | **등가 순열군** | 없음 | ○ | 질의 수 고정 |
| Seg+watershed | 픽셀 | **후처리** | 연결 성분 | — | 무거움 | ○ | 가변 |
| Sat2Graph(GTE) | 셀 | 없음(전역) | 셀당 6슬롯 | **각도 섹터** | 경량 | ✕ | — |
| SAM-Road/DeH4R | 정점 | 없음(전역) | **쌍 분류기** | — | 경량 | ✕ | — |
| AerialLaneNet | 정점 | 모델(2단계) | 다음 정점 **분류** | 방향 고정 | 중간 | △ | 가변 |
| **STELLA (ours)** | 셀 | **모델(사슬)** | R=2 **단위방향** | **최소비용 배정** | 경량 | ○ | 가변 |

열 이름 풀이: **1차 단위** = 모델이 직접 내놓는 것, **정체성** = 선 하나를 하나로 묶는 주체,
**순서 모호성** = 같은 기하에 타깃이 여럿일 때의 처리, **후처리** = 아래 세 등급.

## "후처리" 열은 유무가 아니라 **무엇을 할 수 있는가**로 채운다

우리도 후처리가 있으므로 "없음"이라고 쓰면 거짓이고, 심사에서 바로 반박당한다.
대신 세 등급으로 적는다.

    정체성을 만드는가   후처리가 인스턴스를 만들어 낸다 (면 기반)
    연결을 만드는가     후처리가 선을 잇는다
    중복만 줄이는가     후처리가 있는 것을 지우기만 한다   ← 우리만 이 등급

**우리 방법만 마지막 등급이고, 그것이 이 표의 논지다.** Table IX(후처리 ablation)가
그 주장을 수치로 뒷받침한다 — 우리 후처리는 선을 줄이기만 한다.

## 왜 필요한가

Related Work 를 문단으로만 쓰면 독자가 "그래서 뭐가 다른가"를 스스로 조립해야 한다.
이 표는 **논문의 기여 주장을 열 이름으로 번역한 것**이다 — "순서 모호성 처리"와
"인스턴스 정체성" 두 열이 우리만 다르다는 사실이 표 한 장에서 끝난다.

## 값의 출처 — 손으로 쓴다

실험 결과가 아니라 **저자의 문헌 판단**이다. 그래서 표 내용을 이 스크립트 안의
상수 표로 두고, 코드는 렌더링만 한다. "어느 파일을 고치나"가 하나로 남는 것이 목적이다.

각 칸의 근거가 되는 논문은 `../materials/papers/` 에 PDF 로 있다.
**칸을 고칠 때는 그 PDF 를 다시 확인한다** — 검색 요약으로 채우지 않는다.

## 출력

    table_01/table_01.csv   기계용
    table_01/table_01.md    원고에 붙여 넣는 마크다운

## 상태

**착수 전.** 실험이 필요 없다 — 지금 바로 만들 수 있다.
"""

# from stella.paper.table_base import PaperTable
#
#
# class RepresentationMatrixTable(PaperTable):
#     """문헌 판단으로 채운 비교 매트릭스. 내용은 아래 상수, 코드는 렌더링만."""
#
#     name = "table_01"
#     # 행 내용은 위 docstring 의 표를 그대로 옮긴 상수 리스트로 둔다.
#     # ROWS = [...]
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
