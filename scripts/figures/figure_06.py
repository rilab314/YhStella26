"""Fig. 6 — 접합·교차·이중선 (III-B, 1단, 1×3). **미구현 — 설계만 적어 둔다.**

## 무엇을 그리나

합성 예시 3개. 어려운 기하에서 사슬 규약이 무엇을 보장하는지 보인다.

    (a) T자 접합    곁가지 끝칸이 본선 소유로 남고, **본선 사슬은 끊기지 않는다**
    (b) X자 교차    진 선이 한 칸을 잃고 사슬이 그 칸을 **건너뛴다** (체비셰프 거리 2)
    (c) 이중 중앙선  두 인스턴스가 두 사슬로 따로 남는다

## 왜 필요한가

직전 논문 심사에서 **R3-3b 가 "고리·교차·분기 처리 방법"을 명시적으로 요구**했다.
합성 데이터로 그리면 규약이 무엇을 보장하는지가 **잡음 없이** 보이고,
`tests/` 의 인코더 단위 테스트와 1:1 대응한다 — 그림이 테스트의 시각화가 된다.

**지면이 모자라면 가장 먼저 뺀다** — 본문 서술로 대체 가능한 유일한 그림이다(§6.2 감축안 1번).

## 제작 방식 — 전부 코드다

합성 폴리라인을 직접 만들어 `stella/data/encode.py`(`ChainEncoder`)에 넣고,
나온 `class_map`·`conn_dirs` 를 격자 위에 그린다. 손으로 그린 개념도가 아니라 **인코더의
실제 출력**이어야 한다. 그래야 "규약이 이렇다"가 아니라 "코드가 이렇게 한다"가 된다.

    (a) 세로 직선 + 오른쪽에서 붙는 가지 (T)
    (b) 45도로 교차하는 두 직선 (X)
    (c) 4 px 간격 평행선 2개 (이중 중앙선 — 실제 중앙선 간격 실측 11.6 px 보다 좁게 잡아
        가장 불리한 경우를 보인다)

선별 조건이 없다 — **합성이라 후보를 고르는 것이 아니라 만든다.** 상한도 3장(패널당 1장).

## 입력·출력

    입력   없음 (합성)
    출력   figure_06/figure_06.png · .pdf
           figure_06/figure_06_cells.csv   패널별 채워진 칸 좌표와 연결 방향 —
                                           본문에 "체비셰프 2" 같은 수치를 쓸 때의 근거

## 상태

**착수 전.** 학습·데이터가 필요 없다 — 지금 바로 만들 수 있다.
"""

# from figure_base import PlotFigure
#
#
# class JunctionEncodingFigure(PlotFigure):
#     """합성 T·X·이중선을 실제 인코더에 넣어 격자 위에 그린다."""
#
#     name = "figure_06"
#     DOUBLE_LINE_GAP_PX = 4.0    # 이중선 간격 (실측 중앙값 11.6 px 보다 불리하게)
#     CROSS_ANGLE_DEG = 45.0
#     width_in, height_in = 3.5, 1.4   # 1단 1×3
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
