"""Fig. 7 — 사슬 확장 디코딩 (V절, 2단, 1×4). **미구현 — 설계만 적어 둔다.**

## 무엇을 그리나

    (a) 정점 추출 + 클래스 확률 지도. 시드(국소 피크)를 굵은 점으로.
    (b) **확장 한 스텝을 확대.** 현재 슬롯 방향 c, 후보 b 의 되가리킴 슬롯 n,
        **정렬 콘**과 **마주봄** 조건을 도형으로. **탈락 후보 하나를 회색으로 함께 그린다.**
    (c) 양방향 확장 결과와 끝 셀에서의 끝 연장점.
    (d) 최종 폴리라인.

## 왜 필요한가

"서로가 서로를 향하는가"가 이 디코더의 전부인데, 수식만으로는 **왜 그것이 교차점에서
옆 선으로 갈아타지 않게 하는지** 감이 오지 않는다. (b)의 **탈락 후보**가 그 답이다 —
정렬 콘 안에는 들어왔지만 되가리킴이 어긋나서 떨어진 후보를 같이 그려야 게이트가 보인다.

## 제작 방식 — 실제 디코더의 중간 상태를 뽑는다

개념도로 그리면 "실제로 그 후보가 그렇게 떨어지는가"에 답할 수 없다.
`stella/decode/graph.py`(`ChainDecoder`)를 돌리되 **확장 스텝마다 후보와 판정 결과를
기록하는 훅**을 걸어 그 기록을 그린다. 훅은 이 스크립트가 붙인다 — 디코더 본체는 안 고친다.

기록할 것: 스텝 인덱스 · 현재 셀 · 후보 셀 목록 · 후보별 정렬 코사인 · 마주봄 코사인 ·
거리 · 통과 여부 · 탈락 사유(`stella/decode/stats.py`의 정지 사유와 같은 이름을 쓴다).

## 선별 조건 (AND) — (b)에 쓸 스텝 고르기

    같은 스텝에 후보 >= 2개               탈락 후보를 그려야 하므로 필수
    통과 후보 1개 · 탈락 후보 >= 1개       "골랐다"가 보이려면
    탈락 사유가 **마주봄**일 것            정렬만으로 떨어진 것은 당연해서 그림이 심심하다
    통과·탈락 후보의 정렬 코사인 차 < 0.1   정렬로는 안 갈리고 마주봄으로 갈린 사례여야 한다
    그 스텝이 속한 사슬 길이 >= 20칸        (c)(d)에서 볼 만한 선이어야 한다

## 정렬·상한·파일명

    정렬   |통과 정렬 코사인 − 탈락 정렬 코사인| 오름차순 (덜 갈리는 사례일수록 좋은 예시)
    상한   100장
    이름   {순위:03d}_{타일id}_s{스텝}_dcos-{차이×1000:03d}.png

## 입력

    예측 캐시  .../pred_cache/{실행}/val/{stem}.npz
    디코더     stella.decode.graph.ChainDecoder (기록 훅을 걸어 실행)
    split      val

## 상태

**착수 전.** 캐시만 있으면 만들 수 있다. baseline 이 필요 없다.
"""

# from figure_base import SampleFigure
#
#
# class ChainDecodingFigure(SampleFigure):
#     """디코더 확장 스텝을 기록해, 마주봄 조건으로 후보가 갈린 사례를 고른다."""
#
#     name = "figure_07"
#     split = "val"
#     MIN_CANDIDATES = 2
#     MAX_ALIGN_GAP = 0.10      # 정렬 코사인 차 — 이보다 크면 정렬로 갈린 사례다
#     MIN_CHAIN_CELLS = 20
#     REJECT_REASON = "opposite"  # 탈락 사유가 마주봄인 것만
#
#     def build_figure(self, stem, sample, pred):
#         raise NotImplementedError("설계는 위 docstring 참고")
