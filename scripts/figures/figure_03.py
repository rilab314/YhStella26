"""Fig. 3 — 전체 워크플로 블록 다이어그램 (IV절, 2단). **미구현 — 설계만 적어 둔다.**

## 무엇을 그리나

가로 흐름 한 줄. 화살표마다 **텐서 크기(shape)** 를 적는다.

    영상 (3,768,768)
      → 백본
      → neck (256,192,192)
      → 히트맵 헤드
      → ★ 노드 선택 (학습: GT ∪ 예측 / 추론: 예측만)
      → 토큰 스택 ×6 (전역 어텐션 1층 + 윈도우 어텐션 5층)
      → 출력 헤드들
      → ★ 출력 계약 (class_logit · self_coord · end_logit · exist_logit · conn_dir)
      → 사슬 확장 디코더
      → [{class, points}, ...]

## 왜 필요한가

직전 논문 심사에서 **R1-1 이 정확히 이것을 요구했다** — 단계별 예시 그림이 있어도
"완전한 알고리즘 흐름 · 핵심 입출력 · 결정 규칙"은 대체되지 않는다.
그리고 shape 을 적어야 **"희소 토큰이 어디서 생기는가"** 가 보인다 — 이 구조의 비용이
왜 dense 어텐션보다 싼지가 그림에서 읽혀야 한다.

## 제작 방식 — 그림은 코드가 아니다

블록 도형은 그리기 도구로 만든다. **다만 화살표에 적을 숫자는 코드로 뽑는다** —
손으로 적으면 config 를 바꿨을 때 그림이 조용히 틀린 값을 갖게 된다.

## 이 스크립트가 하는 일

config 하나를 받아 **블록별 텐서 shape·파라미터 수·활성 토큰 수**를 표로 찍는다.
그림에 옮겨 적을 숫자의 단일 출처다.

    입력   configs.base (또는 --config 로 지정)
    계산   각 단계 출력 shape · 모듈별 파라미터 수 · 노드 선택 후 평균 토큰 수(실측)
    출력   figure_03/figure_03_shapes.csv   (열: 단계, 출력 shape, 파라미터 수, 비고)
           화면에도 같은 표를 찍는다

평균 토큰 수는 **val 100장을 실제로 통과시켜** 잰다 — "희소하다"는 주장의 근거 숫자라
설계값이 아니라 실측이어야 한다. (전체 셀 192×192 = 36,864 중 몇 개가 남는가.)

## 상태

**착수 전.** 학습 없이 지금 만들 수 있다 (체크포인트 하나만 있으면 된다).
"""

# from figure_base import PlotFigure
#
#
# class WorkflowShapeTable(PlotFigure):
#     """그림에 적을 shape·파라미터 수·평균 토큰 수를 뽑는다. 도형 자체는 그리기 도구로."""
#
#     name = "figure_03"
#     TOKEN_SAMPLE_COUNT = 100   # 평균 토큰 수를 잴 val 장수
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
