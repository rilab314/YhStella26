"""Fig. 14 — 짧은 선이 어느 단계에서 새는가 (VIII절, 2단, 2패널).
**미구현 — 설계만 적어 둔다.**

## 무엇을 그리나

**(a) 관문별 통과율 (선 그래프).** 정답 칸이 모델을 통과하며 네 관문을 지난다.
길이 구간 네 개마다 통과율이 어떻게 떨어지는지 그린다.

    관문 1  노드 선택        모델이 "여기 뭔가 있다"고 고른 칸
    관문 2  + 히트맵 문턱     그중 확신이 문턱을 넘은 칸
    관문 3  + **전경 판정**   그중 "배경이 아니다"로 판정된 칸   ← 여기서 무너진다
    관문 4  + 비최대 억제     그중 이웃에게 밀리지 않은 칸

**(b) 탈락 칸의 배경 확률 분포.** 관문 3에서 떨어진 정답 칸들이 **얼마나 확신을 갖고**
배경이라 불렸는지. 길이 구간 두 개(0~20칸 · 70칸 이상)를 나란히, 중앙값과 25~75% 범위로.
argmax 경계 0.5 에 수직선을 긋는다.

## ★ 실측 완료

    파일    docs/figures/fig_shortline_bottleneck.png (이미 있다) · val 120장
    (a)     0~20칸  0.947 → 0.889 → **0.291** → 0.268
            70칸+   0.989 → 0.967 → **0.500** → 0.483
    (b)     탈락 칸 배경 확률 중앙값
            0~20칸  0.752 (25~75% 0.605~0.883, n=13,247)
            70칸+   0.737 (25~75% 0.610~0.865, n=68,289)

## 왜 필요한가

"짧은 선이 어렵다"는 관찰(VIII-1)에 **기전**을 붙이는 유일한 그림이다.

    (a)가 병목의 **위치**를 짚는다 — "선이 어디 있나"가 아니라 "이 칸이 배경인가"다.
    (b)가 **왜 그 병목이 재가중이나 문턱 조정으로 안 풀리는지**를 보인다 —
        전경과 배경의 점수 분포가 겹쳐 있어서 문턱을 어디에 그어도 득실이 비슷하다.
        실제로 문턱 0.7 을 걸면 F1 이 −11.5% 로 무너진다(되살아난 정답보다
        같이 딸려 들어온 배경 칸이 훨씬 많다).

이 그림 한 장이 **전경 판정을 겨냥한 시도 7종이 왜 전부 실패했는지**를 요약한다.
그리고 다음 실험(입력 해상도)의 근거가 된다 — 문턱이 아니라 **입력이 문제**라는 것.

## 지면이 모자라면 (a)만 남긴다

§6.2 — 2패널 2단을 1패널 1단으로 줄인다. (b)는 본문 문장으로("탈락 칸의 배경 확률
중앙값은 0.75 로, 41%가 0.7 미만이다").

## 입력·출력

    입력   예측 캐시 .../pred_cache/{채택 실행}/val/{stem}.npz
           GT 캐시   구간별 정답 칸을 세려면 GT 인코딩이 필요하다
           셀 진단   stella/eval/cellstat.py 가 관문 통과 판정의 단일 출처여야 한다
    출력   figure_14/figure_14.png · .pdf
           figure_14/figure_14a.csv   (구간, 관문, 통과율, 표본 칸 수)
           figure_14/figure_14b.csv   (구간, 중앙값, 25%, 75%, n)
    표본   val 120장 — 칸 단위 통계라 120장에서 이미 n 이 만 단위다. 상수로 두고 바꿀 수 있게.

## 상태

**실측 완료 · 재현 스크립트 미구현.** 현재 저장소의 PNG 는 세션 스크래치에서 만든 것이라
재현 스크립트가 없다 — 그 구멍을 메운다. 이 그림은 논문뿐 아니라 **다음 실험의 판정에도
계속 쓰이므로** 재현 스크립트의 가치가 가장 크다.
"""

# from figure_base import PlotFigure
#
#
# class ShortLineBottleneckFigure(PlotFigure):
#     """(a) 관문별 통과율 · (b) 탈락 칸의 배경 확률 분포."""
#
#     name = "figure_14"
#     SAMPLE_TILES = 120
#     BUCKETS = ((0, 20), (20, 40), (40, 70), (70, 10**9))   # 단위: 칸
#     GATES = ("node", "heatmap", "foreground", "nms")       # (a)의 x축
#     DIST_BUCKETS = ((0, 20), (70, 10**9))                  # (b)에 그릴 두 구간
#     ARGMAX_BOUNDARY = 0.5                                  # (b)의 수직선
#     width_in, height_in = 7.16, 2.6                        # 2단
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
