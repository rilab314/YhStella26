"""Table IX — 중복 정리 후처리 ablation (VII-F, 1단) ★. **미구현 — 설계만 적어 둔다.**

## 무엇이 들어가나

후처리 두 단계를 켜고 끄며 네 조건을 잰다. **마지막 열이 이 표의 논지다.**

| 조건 | F1 | 정밀도 | 재현율 | 중복 비율 | **주입 천장** |
| --- | --- | --- | --- | --- | --- |
| 후처리 없음 | 0.3603 | 0.4042 | 0.3250 | **12.9%** | **0.9414** |
| + 정점 비최대 억제 | 0.3734 (+3.6%) | 0.4402 | 0.3243 (−0.2%) | 5.9% | **0.9414 (0.0%)** |
| + 중복 정리 | 0.3798 (+5.4%) | 0.4689 | 0.3192 (−1.8%) | 2.0% | 0.9368 (−0.5%) |
| **+ 둘 다 (채택)** | **0.3860 (+7.1%)** | **0.4859** | 0.3202 | **0.5%** | 0.9368 (−0.5%) |

종류별 평균 F1 은 같은 순서로 0.2828 / 0.2906 / 0.2952 / **0.2984**.

**위 값은 v1.1 val 400장(중복 비율은 120장)에서 잰 것이다.** v1.2 로 바꿨고 논문은 test 를
보고하므로 **다시 잰다.** 표의 구조와 논지는 그대로다.

## "중복 선 비율"의 정의를 표 각주에 쓴다

같은 클래스의 다른 예측선과 **자기 길이의 80% 이상이 8 px 안에서 나란히 가는**
(평가와 같은 각도 게이트) 예측선의 비율.

실측 12.9% → 0.5%, 겹친 쌍의 **간격 중앙값은 1.74 px → 4.11 px** 로 벌어진다 —
남는 것은 같은 차선 위의 이중 그리기가 아니라 **원래 가까운 이웃**이다.
즉 지운 것이 정확히 노린 것이었다. 영상당 예측선 수는 31.5 → 27.1개로 **줄기만 한다.**

## 이 표에서 세 가지가 읽힌다

① **두 단계는 서로를 대체하지 않고 더해진다** — 원인이 두 층에 걸쳐 있다.
   정점을 한 줄로 줄여도 남는 중복이 있고, 선을 지워도 못 잡는 것이 있다.

② **정점 억제는 천장을 전혀 깎지 않는다** (소수점 넷째 자리까지 동일).
   정답에는 넓은 전경 띠가 없으므로(선 하나당 칸 하나가 주인) 억제할 것이 없기 때문이다 —
   **표현 단계의 처방은 정답 위에서 아무것도 건드리지 않는다**는 직접 증거다.

③ **후처리는 −0.5%를 치른다.** 그것이 이 논문이 공개하는 후처리의 대가다.

## 왜 필요한가

① **후처리의 이득과 대가를 격리한다.** 이 논문이 baseline 을 비판하는 근거가
   "후처리가 성능을 만든다"이므로, **우리 후처리의 몫을 숨기면 같은 비판을 되받는다.**

② **원인을 어디서 막는 것이 옳은가**에 답한다. 중복의 원인은 모델이 정답보다 넓은 띠를
   전경으로 부르는 것이다(선택된 칸이 정답 칸의 4배). 정점 단계에서 막으면 원인에 가깝지만
   정점을 잃을 위험이 있고, 선 단계에서 지우면 안전하지만 계산이 는다. **실험으로 정한다.**

③ `ms/타일` 열이 "가벼운 후처리"라는 주장을 수치로 뒷받침한다.

## 미리 정한 판정 기준 (표를 만들기 전에 정해 둔 것)

후처리는 **정밀도를 올리고 재현율을 떨어뜨리지 않아야** 한다 — 중복 제거는 정의상 재현율을
건드릴 이유가 없다. 재현율이 밴드(±2%) 밖으로 내려가면 그 단계는 중복이 아니라
**진짜 선을 지우고 있는 것**이므로 기각한다. 스크립트가 이 검사를 하고 결과를 찍는다.

## 옛 수치와 다른 이유를 각주에 남긴다

같은 정의로 잰 값이 예전에는 18.5% 였다. 그때는 디코더 반경이 12 px 이었고, 그 뒤 반경 5 px ·
최소 길이 8칸 · 조각 병합 24 px 가 채택됐다. **디코더 쪽 처방이 이미 중복의 3분의 1을
지웠고**(18.5% → 12.9%), 후처리는 남은 것을 지운다.

## 값의 출처

    예측 캐시를 같은 실행에서 뜨고, **후처리 설정만 바꿔** 네 번 평가한다
    (stella/decode/sweep.py — GPU 불필요).
    정답 주입 천장은 같은 후처리 설정으로 GT 캐시를 평가한 값이다.
    split  **test**

## 출력

    table_09/table_09.csv · table_09.md

## 상태

**v1.1 val 에서 실측 완료 · 스크립트 미구현.** 최종 채택 설정의 캐시가 뜨면
v1.2 test 로 다시 재서 채운다. 후처리 손잡이가 이미 있으므로 **가장 빨리 채울 수 있는 표**다.
"""

# from table_base import PaperTable
#
#
# class PostprocessAblationTable(PaperTable):
#     """후처리 두 단계를 켜고 끄며 이득과 대가를 격리한다."""
#
#     name = "table_09"
#     SPLIT = "test"
#     CONDITIONS = ("none", "vertex_nms", "dedup", "both")
#     METRICS = ("f1", "precision", "recall", "redundant_rate", "oracle_f1", "ms_tile")
#     RECALL_BAND = 0.02          # 재현율이 이 밖으로 내려가면 기각 (미리 정한 기준)
#     REDUNDANT_OVERLAP = 0.80    # 중복 선 판정: 자기 길이의 이 비율 이상이 겹침
#     REDUNDANT_GAP_PX = 8.0
#
#     def collect(self):
#         raise NotImplementedError("설계는 위 docstring 참고")
